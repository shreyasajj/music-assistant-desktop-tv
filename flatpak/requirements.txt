# Runtime dependencies for the Flatpak build. Feed this to flatpak-pip-generator
# to produce the pinned, hashed python3-requirements.json (see the manifest header).
PySide6>=6.6
qasync>=0.27
music-assistant-client>=1.0
aiohttp>=3.9
qrcode>=7.4
numpy>=1.26
sounddevice>=0.4
